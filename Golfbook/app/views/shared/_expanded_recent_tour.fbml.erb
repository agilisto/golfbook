<div>
	<span class="name" style="font-size:11px;font-weight:bold">
		<%= link_to expanded_recent_tour.name, :controller => :tours, :action => :show, :id => expanded_recent_tour.id %>
	</span><br/>
	<span class="name">
		<a href="<%= profile_view_url :id=>expanded_recent_tour.user.id, :only_path => true%>">
			<fb:name uid='<%= expanded_recent_tour.user.facebook_uid %>' linked="false" useyou='false' />
		</a>
	</span>
	<span class="played"><%= time_ago_in_words(expanded_recent_tour.created_at) %> ago</span>
</div>
