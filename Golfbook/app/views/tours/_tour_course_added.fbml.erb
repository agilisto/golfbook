has added the 
<%=link_to @course.name, :controller => :courses, :action => :show, :id => @course.id%>
course to the 
<%=link_to @tour.name, :controller => :tours, :action => :show, :id => @tour.id%> golf tour.