<h3>The Course Menu</h3>
<%if !@course.awaiting_review%>
<div class="communication_item send_message clearfix">
    <%if @user.has_played_course? @course%>
        You have played this course
    <%else%>
        <%= link_to 'Mark as played', course_have_played_url(:id => @course.id, :only_path => true) %>
    <%end%>
</div>
<div class="communication_item send_message clearfix">
	<%= link_to 'Post Score', course_add_round_url(:id => @course.id, :only_path => true) %>
</div>
<div class="communication_item send_message clearfix">
	<%= link_to 'Want to Play', course_add_wishlist_url(:id => @course.id, :only_path => true) %>
</div>
<div class="communication_item send_message clearfix">
	<%= link_to 'Host Competition', course_add_competition_url(:id => @course.id, :only_path => true) %>
</div>
<div class="communication_item send_message clearfix">
	<%= link_to 'Add to Tour', url_for(:controller => :tours, :action => :add_course_to_tour, :course_id => @course.id, :only_path => true) %>
</div>
<div class="communication_item send_message clearfix">
	<%= link_to 'Schedule Game', url_for(:controller => :courses, :action => :schedule_game, :id => @course.id, :only_path => true) %>
</div>
<%else%>
  <div class="communication_item send_message clearfix">
    This course is still awaiting review and no actions can currently be taken.
  </div>
<%end%>